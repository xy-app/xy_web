# 切换标签页

在多标签页场景下，通过标题关键词或 URL 匹配切换当前活动的浏览器标签页。

## 运行参数

* **驱动 (`driver`)**
> 浏览器驱动对象或会话标识。如果不填写，默认使用当前活动浏览器会话。

* **标题或 URL 匹配规则 (`title_or_url`)**
> 目标标签页的页面标题关键词或 URL 子串。如果留空，默认切换至下一个标签页。

## 输出

* **目标标签页标识 (`target_id`)**
> 成功激活的浏览器标签页唯一 Target ID。
