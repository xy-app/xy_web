# 处理网页弹窗

自动处理由页面 JavaScript 触发的原生弹窗（如 `alert()`、`confirm()`、`prompt()`）。

## 运行参数

* **驱动 (`driver`)**
> 浏览器驱动对象或会话标识。如果不填写，默认使用当前活动浏览器会话。

* **响应操作 (`accept`)**
> 是否确认或接受弹窗。填 `true` 为确认/接受（OK），填 `false` 为取消/拒绝（Cancel）。

* **输入文本 (`prompt_text`)**
> 当弹窗为 `prompt()` 输入框时，自动填入的文本内容。

## 输出
> 无
