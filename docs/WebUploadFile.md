# 静默上传文件

通过底层浏览器协议直接向网页文件输入控件注入本地文件路径，彻底绕过系统原生文件选择弹窗。

## 运行参数

* **驱动 (`driver`)**
> 浏览器驱动对象或会话标识。如果不填写，默认使用当前活动浏览器会话。

* **目标元素 (`element`)**
> 文件输入框元素选择器（默认为 `input[type='file']`）。

* **本地文件路径 (`file_path`)**
> 本地待上传文件的绝对路径。支持多个文件，以分号 `;` 分隔。

## 输出
> 无
